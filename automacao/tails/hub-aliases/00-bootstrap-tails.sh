#!/usr/bin/env bash
# aliases — Passos 2–4 MANUAIS (persistência, dotfiles, senha admin no boot).
# Nao ha script: siga o arquivo canonico do curso (Passos 2–4).
echo "Passo MANUAL (2–4): Persistencia + Dotfiles + senha admin no boot."
echo "Depois rode: aliases/01-check-tails-environment.sh"
exit 1
