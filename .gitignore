# --- Repo público = SÓ O CURSO. Camada de manutenção/auditoria fica LOCAL. ---
# (O Claude Code lê o CLAUDE.md do disco mesmo gitignorado.)
CLAUDE.md
.claude/

# --- Doc interna equipe dev · prefixo do repo evita colisão em backups mistos ---
Hub-equipe-dev/
MANIFEST.txt

# Mantenedor off remoto (jul/2026)
/docs/pgp-check-log.md

# --- Agentes internos Claude Code (ficam só no disco local) ---
agents/

# --- IDE (planos locais vivem em Privacy-OS-Hub-equipe-dev/arquivo-morto/cursor-plans/) ---
.cursor/

# --- Arquivos compactados / binários grandes ---
*.zip
*.7z
*.rar
*.tar
*.tar.gz
*.tar.bz2

# --- Cruft de sistema operacional ---
Thumbs.db
desktop.ini
.DS_Store
*~

# --- Backups da carteira Haveno (nunca versionar) ---
*.tar.gz.gpg
*.sha256
Backups/

# --- Editores / IDE ---
.vscode/
.idea/
